apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wisecow-cert
  namespace: wisecow
spec:
  secretName: wisecow-tls
  issuerRef:
    name: wisecow-selfsigned
    kind: Issuer
  commonName: wisecow.local
  dnsNames:
    - wisecow.local